export interface UserState {
  height: string;
  weight: string;
  age: string;
  gender: string;
  activityLevel: string;
  healthConditions: string[];
  goal: string; // User's fitness goal
  fitnessLevel: string; // Fitness level selected by the user
}

